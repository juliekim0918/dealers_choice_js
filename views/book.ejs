<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Books I've Read in 2021</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Proza+Libre:ital,wght@0,400;0,700;1,500&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="../public/styles.css" />
  </head>
  <body>
    <div class="book-container">
      <h3 class="title"><%= currBook.title%></h3>
      <h4 class="author"><%= currBook.author_name %></h4>
      <div class="genre-container">
        <% currBook.genres.split(', ').forEach((genre) => { %>
          <p class="genre"><%= genre %></p>              
        <% }) %>
      </div>
      <p class="summary"><%= currBook.summary %></p>
      <p class="date-read"><span>Date finished: </span><%= currBook.datefinished %></p>
      <p class="my-rating"><span>My rating: </span><%= currBook.rating %> / 5</p>
      <div class="btn-container">
        <a href="/" class="go-back-btn"><p> < Go back</p></a>
        <form id="deleteBook" method="POST" action="/books/<%=currBook.id%>?_method=DELETE" >
          <button>Delete this book</button>
      </div>
      </form>
    </div>
  </body>
</html>
